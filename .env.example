# ===========================================
# CAPITAL BTC AI - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control

# CRM Access Password
# This protects the /crm route
CRM_PASSWORD=your_secure_password_here

# Google Apps Script URL for Lead Storage
# Deploy your Google Script and paste the URL here
# Leave empty for localStorage fallback (development)
GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# ===========================================
# META PIXEL (FACEBOOK PIXEL) CONFIGURATION
# ===========================================
# Get your Pixel ID from Meta Business Manager:
# https://business.facebook.com/events_manager
#
# Steps to get your Pixel ID:
# 1. Go to Meta Business Suite > Events Manager
# 2. Click "Connect Data Sources" > "Web" > "Meta Pixel"
# 3. Name your pixel and click "Create Pixel"
# 4. Copy the Pixel ID (15-16 digit number)
#
# Leave empty to disable tracking (useful for development)
VITE_META_PIXEL_ID=

# ===========================================
# TRACKED EVENTS REFERENCE
# ===========================================
# The following events are automatically tracked:
#
# PageView        - When user loads the page (once per session)
# ViewContent     - When landing page is viewed (once per session)
# InitiateCheckout - When qualification form is opened (once per session)
# FormStep        - Custom event for each form step completed
# Lead            - When form is successfully submitted
# CompleteRegistration - When form submission succeeds
# DownsellActivated - Custom event when downsell offer is accepted
#
# All events include event_id for deduplication with Conversions API
